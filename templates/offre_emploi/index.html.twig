{% extends 'base.html.twig' %}

{% block title %}Liste des OffreEmplois{% endblock %}

{% block body %}
<div class="dashboard-container">
    <div class="top-bar">
        <h1>Liste des OffreEmplois</h1>
        <div class="top-actions">
            <a href="{{ path('app_offre_emploi_new') }}" class="btn btn-primary">
                <i class="fas fa-plus"></i> Créer Nouveau
            </a>
        </div>
    </div>

    <div class="stat-card">
        <table class="table">
            <thead>
                <tr>
                    <th>Id offre</th>
                    <th>Titre poste</th>
                    <th>Departement</th>
                    <th>Statut offre</th>
                    <th>Salaire propose</th>
                    <th>Date creation</th>

                    <th>Actions</th>
                </tr>
            </thead>
            <tbody>
            {% for offre_emploi in offre_emplois %}
                <tr>
                    <td>{{ offre_emploi.id_offre }}</td>
                    <td>{{ offre_emploi.titre_poste }}</td>
                    <td>{{ offre_emploi.departement }}</td>
                    <td>{{ offre_emploi.statut_offre }}</td>
                    <td>{{ offre_emploi.salaire_propose }}</td>
                    <td>{{ offre_emploi.date_creation ? offre_emploi.date_creation|date('Y-m-d') : '' }}</td>

                    <td class="talent-actions">
                        <a href="{{ path('app_offre_emploi_show', {'id_offre': offre_emploi.id_offre}) }}" class="btn-icon"><i class="fas fa-eye"></i></a>
                        <a href="{{ path('app_offre_emploi_edit', {'id_offre': offre_emploi.id_offre}) }}" class="btn-icon"><i class="fas fa-edit"></i></a>
                        {{ include('offre_emploi/_delete_form.html.twig') }}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="10">Aucun enregistrement trouvé</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}