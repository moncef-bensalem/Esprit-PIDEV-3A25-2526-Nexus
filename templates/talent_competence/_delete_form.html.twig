<form method="post" action="{{ path('app_talent_competence_delete', {'id': talent_competence.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ talent_competence.id) }}">
    <button class="btn">Delete</button>
</form>
