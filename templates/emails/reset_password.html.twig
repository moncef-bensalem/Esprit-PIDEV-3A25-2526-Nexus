<p>Hello,</p>
<p>Click the link below to reset your NEXUS password:</p>
<p><a href="{{ url('app_reset_password', {token: resetToken.token}) }}">Reset my password</a></p>
<p>This link expires in {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.</p>
