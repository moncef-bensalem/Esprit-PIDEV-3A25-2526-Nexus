{% for user in users %}
    <tr>
        <td style="color: rgba(255,255,255,.4);">#{{ user.id }}</td>
        <td>
            <div style="font-weight: 600;">{{ user.firstName }} {{ user.lastName }}</div>
        </td>
        <td>
            <div style="font-size: .8rem; color: rgba(255,255,255,.6);">
                <i class="fa fa-envelope" style="margin-right:4px; opacity:.7;"></i>{{ user.email }}
            </div>
        </td>
        <td>
            {% for role in user.roles %}
                <span class="nx-badge {% if role == 'ROLE_ADMIN' %}admin{% endif %}">
                    {{ role|replace({'ROLE_': ''}) }}
                </span>
            {% endfor %}
        </td>
        <td>
            {% if user.isActive %}
                <span class="nx-status active">Actif</span>
            {% else %}
                <span class="nx-status inactive">Inactif</span>
            {% endif %}
        </td>
        <td>
            <div class="nx-row-actions">
                <form method="post" action="{{ path('admin_user_toggle', {id: user.id}) }}" style="margin:0;">
                    <input type="hidden" name="_token" value="{{ csrf_token('toggle_user_' ~ user.id) }}">
                    {% if user.isActive %}
                        <button type="submit" class="nx-btn-icon toggle-off" title="Désactiver le compte">
                            <i class="fa fa-ban"></i>
                        </button>
                    {% else %}
                        <button type="submit" class="nx-btn-icon toggle-on" title="Activer le compte">
                            <i class="fa fa-check"></i>
                        </button>
                    {% endif %}
                </form>
                <a href="{{ path('admin_user_stats', {id: user.id}) }}" class="nx-btn-icon" title="Stats">
                    <i class="fa fa-chart-line"></i>
                </a>
                <a href="{{ path('admin_user_edit', {id: user.id}) }}" class="nx-btn-icon edit" title="Modifier">
                    <i class="fa fa-pen"></i>
                </a>
            </div>
        </td>
    </tr>
{% else %}
    <tr>
        <td colspan="6">
            <div class="nx-empty">
                <i class="fa fa-folder-open"></i>
                <div>Aucun utilisateur trouvé.</div>
            </div>
        </td>
    </tr>
{% endfor %}
