{% extends 'base.html.twig' %}

{% block title %}Ajouter un talent - NEXUS{% endblock %}

{% block content %}
<div class="dashboard-container">
    <div class="top-bar">
        <h1><i class="fa-solid fa-user-plus"></i> Ajouter un talent</h1>
        <div class="top-actions">
            <a href="{{ path('app_talent_index') }}" class="btn"><i class="fa-solid fa-arrow-left"></i> Retour à la liste</a>
        </div>
    </div>

    <div class="form-container">
        {{ include('talent/_form.html.twig', {'button_label': 'Ajouter le talent'}) }}
    </div>
</div>
{% endblock %}
