{% extends 'admin_layout.html.twig' %}

{% block title %}Éditer Departement{% endblock %}

{% block body %}
<div class="dashboard-container">
    <div class="top-bar">
        <h1>Éditer Departement</h1>
        <div class="top-actions">
            <a href="{{ path('app_departement_index') }}" class="btn">
                <i class="fas fa-arrow-left"></i> Retour
            </a>
            {{ include('departement/_delete_form.html.twig') }}
        </div>
    </div>

    <div class="stat-card" style="text-align: left; max-width: 800px; margin: 0 auto;">
        {{ include('departement/_form.html.twig', {'button_label': 'Mettre à jour'}) }}
    </div>
</div>
{% endblock %}
